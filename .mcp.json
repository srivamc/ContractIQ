{
  "mcpServers": {
    "contractiq-mcp": {
      "command": "python",
      "args": ["mcp_server/contractiq_mcp_server.py"],
      "description": "ContractIQ MCP Server - AI-powered test generation, spec analysis, and requirements processing",
      "env": {
        "CONTRACTIQ_MCP_PORT": "8765",
        "CONTRACTIQ_LOG_LEVEL": "INFO"
      }
    },
    "memory-keeper": {
      "command": "python",
      "args": ["mcp_server/memory_keeper.py"],
      "description": "ContractIQ Memory Keeper - Cross-session context persistence, knowledge base, and semantic search",
      "env": {
        "MEMORY_DB_PATH": ".contractiq/memory.db",
        "MEMORY_VECTOR_PATH": ".contractiq/vectors"
      }
    },
    "playwright-mcp": {
      "command": "npx",
      "args": ["@playwright/mcp@latest"],
      "description": "Playwright MCP Server - Browser automation for UI testing and self-healing locator repair",
      "env": {
        "PLAYWRIGHT_HEADLESS": "true",
        "PLAYWRIGHT_BROWSER": "chromium"
      }
    }
  },
  "globalSettings": {
    "project": "contractiq-agentic-qe",
    "version": "1.0.0",
    "aiModel": "claude-3-5-sonnet-latest",
    "contextWindow": 200000,
    "maxTokensPerRequest": 8192
  }
}
